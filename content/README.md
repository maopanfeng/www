# Published Website Notes

This folder is the public writing area for `www.maopanfeng.com`.

Use:

- `cn/posts/` for Chinese posts
- `en/posts/` for English posts

The website publisher syncs only the public `cn` and `en` folders from here into `/Users/ben/www`.
